services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: react-app:latest
    container_name: react-app
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - devops-network

networks:
  devops-network: